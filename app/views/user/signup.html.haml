= form_for @user, :url => {:action => "signup"} do |f|
  - if @user.errors.any?
    - @user.errors.full_messages.each do |msg|
      = msg
  = f.text_field :username, :size => 20
  %br
  = f.password_field :pass, :size => 20
  %br
  = f.submit "Signup"
