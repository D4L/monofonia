%strong Hello and welcome to monofonia
.groupControl
  - if @_current_user
    = render "group/group_table"
    = link_to "New Group", group_create_url
.userControl
  - if @_current_user
    = render "user/control_panel"
  - else
    = render "user/login_panel"
